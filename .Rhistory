modelCall(api_key = Sys.getenv("GROQ_API_KEY"))
library(groqR)
modelCall(api_key = Sys.getenv("GROQ_API_KEY"))
groqR:::runAddin_ask()
devtools::install_github("GabrielKaiserQFin/groqR")
library(devtools)
install.packages("devtools")
library(devtools)
devtools::install_github("GabrielKaiserQFin/groqR")
modelCall(api_key = Sys.getenv("GROQ_API_KEY"))
groqR:::runAddin_ask()
install.packages(c("httr", "jsonlite", "yaml"))
install.packages(c("httr", "jsonlite", "yaml"))
groqR:::runAddin_ask()
# Définir les paramètres Groq, y compris systemRole
groq_params <- list(
api_key = Sys.getenv("GROQ_API_KEY"),
systemRole = "Tu es un assistant IA qui aide avec l'analyse de données en R."
)
# Si vous utilisez une fonction comme ask() du package groqR
ask("Votre question ici", params = groq_params)
library(groqR)
Sys.setenv(GROQ_API_KEY)
# Définir les paramètres Groq, y compris systemRole
groq_params <- list(
api_key = Sys.getenv("GROQ_API_KEY"),
systemRole = "Tu es un assistant IA qui aide avec l'analyse de données en R."
)
# Si vous utilisez une fonction comme ask() du package groqR
ask("Votre question ici", params = groq_params)
# Définir les paramètres Groq, y compris systemRole
groq_params <- list(
api_key = Sys.getenv("GROQ_API_KEY"),
GROQ_systemRole = "Tu es un assistant IA qui aide avec l'analyse de données en R."
)
# Si vous utilisez une fonction comme ask() du package groqR
ask("Votre question ici", params = groq_params)
